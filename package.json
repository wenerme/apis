{
  "name": "@wener/apis",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn --cwd packages/server build",
    "build:public": "yarn --cwd packages/ui run storybook:build && cp -r packages/ui/storybook packages/server/public/storybook && yarn --cwd packages/utils run doc && yarn --cwd packages/tinyrpc run doc && rm -rf packages/server/public/docs/@wener && mkdir -p packages/server/public/docs/@wener && cp -r packages/utils/docs packages/server/public/docs/@wener/utils && cp -r packages/tinyrpc/docs packages/server/public/docs/@wener/tinyrpc",
    "dev": "yarn --cwd packages/server dev",
    "format": "yarn workspaces run format",
    "build:ui": "yarn --cwd packages/ui build",
    "build:tinyrpc": "yarn --cwd packages/tinyrpc build",
    "build:utils": "yarn --cwd packages/utils build",
    "pub": "yarn build:tinyrpc && yarn build:utils && yarn build:ui && yarn lerna publish prerelease --registry https://registry.npmjs.org/  --no-push",
    "pub:build": "yarn build:tinyrpc && yarn build:utils && yarn build:ui && yarn --cwd packages/server tsc --noEmit",
    "sort": "npx sort-package-json package.json tsconfig*.json packages/*/package.json packages/*/tsconfig*.json && prettier --write package.json tsconfig*.json packages/*/package.json packages/*/tsconfig*.json",
    "start": "yarn --cwd packages/server start",
    "start:server": "yarn --cwd packages/server start",
    "test": "yarn --cwd packages/server test"
  },
  "resolutions": {
    "prettier": "^2.0.0",
    "styled-components": ">= 5",
    "svgo": "^1.3.2"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.25.0",
    "@typescript-eslint/parser": "^2.25.0",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.1",
    "eslint-plugin-jest": "^23.8.2",
    "eslint-plugin-react": "^7.19.0",
    "eslint-plugin-react-hooks": "^3.0.0",
    "husky": "^4.2.3",
    "lerna": "^3.20.2",
    "lint-staged": "^10.0.8",
    "prettier": "^2.0.2"
  },
  "engines": {
    "node": "12"
  }
}
