# Template file for root package

```bash
# lerna
yarn add -W -D lerna

# Prettier
yarn add -W -D prettier

# lint-staged
yarn add -W -D husky lint-staged

# eslint
yarn add -W -D eslint eslint-config-prettier eslint-plugin-jest eslint-plugin-react eslint-plugin-react-hooks
# typescript-eslint
yarn add -W -D @typescript-eslint/eslint-plugin @typescript-eslint/parser
```
