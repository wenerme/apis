import { LazyComponentType } from '../types/types';

export function resolver({ type }) {
  const c: LazyComponentType | null = null;

  // @formatter:off
  // prettier-ignore
  switch (type) {
    // generated:begin
    // generated:end
    // @formatter:on
    default:
  }
  return c;
}
