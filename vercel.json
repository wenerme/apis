{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/packages/server/$1" }],
  "builds": [
    {
      "src": "packages/server/next.config.js",
      "use": "@vercel/next",
      "config": { "maxLambdaSize": "100mb" }
    }
  ]
}
